# Backend URLs
# For local development (used when NODE_ENV !== 'production')
BACKEND_URL_DEV=

# For production (used when NODE_ENV === 'production')
# Set this in Vercel dashboard for production deployments
BACKEND_URL_PROD=

# Base path (optional)
NEXT_PUBLIC_BASE_PATH=

# WIP